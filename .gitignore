# Downloaded upstream repo (https://github.com/MIT-LCP/mimic-code) — not tracked here.
# Must be present locally in folder format (extracted, not .zip).
mimic-code-main/

# Source archives — already extracted (dataset -> mimic-iv-clinical-database-demo-2.2/,
# docs -> mimic-iv-docs/). Kept locally but not tracked.
mimic-iv-clinical-database-demo-2.2.zip
mimic.mit.edu-main.zip

# Secrets — real env file (use .env.example as the template). Never commit keys.
.env
.env.local
.env.*.local

.env

# Internal deploy runbook — kept local, not published to the public repo.
DEPLOY.md

# Internal working files — kept local, not published to the public (judge-facing) repo.
CLAUDE.md
PROGRESS.md
TENTATIVE_AGILE_PLAN.md
docs/PITCH_SCRIPT.md
docs/SLIDE_PROMPT.md
docs/SUBMISSION.md